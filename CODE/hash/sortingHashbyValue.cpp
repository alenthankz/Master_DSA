#include<bits/stdc++.h>
using namespace std;



/*
    vector< pair<int,int> > arr;
    for(auto it : mp){
        arr.push_back({it.first,it.second});
    }
    sort(arr.begin(),arr.end(),myComp);
    mp.clear();
    for(auto p : arr){
        mp[p.first]=p.second;
    }
*/